name: return_success_or_error_example
description: Pure Dart example demonstrating the return_success_or_error package.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

dependencies:
  return_success_or_error:
    path: ../

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.0
