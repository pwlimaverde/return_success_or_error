name: example_app_flutter_get_it
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.9
  return_success_or_error:
    path: ../../
  connectivity_plus: ^7.1.1
  flutter_getit: ^3.0.1
  get_it: ^7.7.0 # limitado pelo flutter_getit 3.0.1 (requer get_it 7.x)
  signals_flutter: ^7.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
