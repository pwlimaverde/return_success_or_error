name: return_success_or_error
description: Usecase abstraction returning success or error, with the error type closed per feature for exhaustive handling.
version: 3.0.0
homepage: https://github.com/pwlimaverde/return_success_or_error
repository: https://github.com/pwlimaverde/return_success_or_error
issue_tracker: https://github.com/pwlimaverde/return_success_or_error/issues
topics:
  - clean-architecture
  - usecase
  - result
  - error-handling

environment:
  sdk: ^3.12.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.0
  mocktail: ^1.0.5
